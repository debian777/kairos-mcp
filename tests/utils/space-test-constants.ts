/**
 * Canonical train/tune `space` values for integration tests (match Keycloak full group paths).
 */

/** Full Keycloak group path for the CI shared space (JWT `groups` / resolve-space-param). */
export const CI_TEST_SPACE_PARAM = '/shared/ci-test';

Plain-text artifact for MIME text/plain.
See conf/app-config.toml (message) and conf/routes.yaml (greeting). Run scripts/hello.* from the skill root, or set KAIROS_MIME_SAMPLE_ROOT to this directory’s absolute path.

{{- if and .Values.monitoring.serviceMonitor.redis.enabled .Values.monitoring.serviceMonitor.redis.serviceNamespace }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-redis
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/part-of: kairos-fullstack
    {{- with .Values.monitoring.serviceMonitor.redis.release }}
    release: {{ . }}
    {{- end }}
spec:
  namespaceSelector:
    matchNames:
      - {{ .Values.monitoring.serviceMonitor.redis.serviceNamespace }}
  selector:
    matchLabels:
      {{- (.Values.monitoring.serviceMonitor.redis.serviceSelector | default (dict "app" "redis") | toYaml) | nindent 6 }}
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.monitoring.serviceMonitor.redis.interval }}
{{- end }}

{{- if and .Values.monitoring.serviceMonitor.keycloak.enabled .Values.monitoring.serviceMonitor.keycloak.serviceNamespace }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-keycloak
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/part-of: kairos-fullstack
    {{- with .Values.monitoring.serviceMonitor.keycloak.release }}
    release: {{ . }}
    {{- end }}
spec:
  namespaceSelector:
    matchNames:
      - {{ .Values.monitoring.serviceMonitor.keycloak.serviceNamespace }}
  selector:
    matchLabels:
      {{- (.Values.monitoring.serviceMonitor.keycloak.serviceSelector | default (dict "app.kubernetes.io/name" "keycloak") | toYaml) | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.monitoring.serviceMonitor.keycloak.interval }}
{{- end }}

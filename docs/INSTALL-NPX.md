# Moved

That content is folded into:

- **[install/cursor-mcp.md](install/cursor-mcp.md)** — HTTP MCP in Cursor (not stdio; no `npx` MCP subprocess)
- **[CLI.md](CLI.md)** — `npx @debian777/kairos-mcp` and the `kairos` CLI

See the [install index](install/README.md) for all installation documentation.

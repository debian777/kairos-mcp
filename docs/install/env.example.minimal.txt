# ------------------------------------------------------------
# KAIROS MCP — Minimal (app + Qdrant only)
# Copy to .env. No Redis required (in-memory); no auth; no Keycloak.
# ------------------------------------------------------------

PORT=3300
QDRANT_URL=http://127.0.0.1:6333
QDRANT_COLLECTION=kairos

# One of:
OPENAI_API_KEY=sk-proj-xxxxxxxx
# or TEI_BASE_URL=http://127.0.0.1:8080

# Optional: Redis (omit for in-memory)
# REDIS_URL=redis://127.0.0.1:6379

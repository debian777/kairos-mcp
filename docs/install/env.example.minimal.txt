# ------------------------------------------------------------
# KAIROS MCP — Minimal (app + Qdrant only)
# Copy to .env. No Redis required (in-memory); no auth; no Keycloak.
# ------------------------------------------------------------

# OpenAI API compatible endpoint (optional). Base URL only — app appends /v1/embeddings.
# Ollama: uncomment one OPENAI_API_URL, set OPENAI_EMBEDDING_MODEL and OPENAI_API_KEY=ollama.
# App on host:
# OPENAI_API_URL=http://localhost:11434
# App in Docker, Ollama on host (Mac/Windows):
# OPENAI_API_URL=http://host.docker.internal:11434
# OPENAI_EMBEDDING_MODEL=nomic-embed-text

# OpenAI API (required). Use "ollama" for local Ollama (no real key).
OPENAI_API_KEY=sk-proj-xxxxxxxx


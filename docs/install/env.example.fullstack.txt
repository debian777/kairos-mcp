# ------------------------------------------------------------
# KAIROS MCP — Full stack (infra + app + auth)
# Copy to .env for Docker full stack or CI. Set secrets before use.
# ------------------------------------------------------------

# --- Infra (Compose: Redis, Qdrant, Postgres, Keycloak) ---
KEYCLOAK_ADMIN_PASSWORD=
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_DB_PASSWORD=
QDRANT_API_KEY=
QDRANT_COLLECTION=kairos_dev
QDRANT_URL=http://127.0.0.1:6333
# For Docker fullstack use: REDIS_URL=redis://redis:6379
REDIS_URL=redis://127.0.0.1:6379

# --- App (dev/local or CI) ---
PORT=3300
METRICS_PORT=9390
NODE_ENV=development
LOG_LEVEL=info
LOG_FORMAT=json
KAIROS_REDIS_PREFIX=kairos:dev:
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536
EMBEDDING_PROVIDER=openai
QDRANT_RESCORE=true

# --- Auth (AUTH_ENABLED=true) ---
AUTH_ENABLED=true
SESSION_SECRET=
SESSION_MAX_AGE_SEC=604800
AUTH_CALLBACK_BASE_URL=http://localhost:3300
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=kairos-dev
KEYCLOAK_CLIENT_ID=kairos-mcp
KEYCLOAK_INTERNAL_URL=http://keycloak:8080
AUTH_MODE=oidc_bearer
AUTH_TRUSTED_ISSUERS=http://localhost:8080/realms/kairos-dev
AUTH_ALLOWED_AUDIENCES=kairos-mcp

# QA environment (copy to .env.qa and customize).
# For qa:deploy / qa:test with auth: set KEYCLOAK_URL so tests can get a Bearer token.
# Keycloak runs in Docker (profile qa); from the host use localhost.

PORT=3500
METRICS_PORT=9590

# Auth: when true, app and tests require a token. Tests get it from Keycloak at KEYCLOAK_URL.
# AUTH_ENABLED=true
# KEYCLOAK_URL=http://localhost:8080
# KEYCLOAK_REALM=kairos-qa
# KEYCLOAK_CLIENT_ID=kairos-mcp

# Qdrant/Redis: override if not using defaults (app-qa uses service names qdrant/redis in compose).
# QDRANT_URL=http://qdrant:6333
# REDIS_URL=redis://redis:6379

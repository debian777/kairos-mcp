# Bundles (reserved)

This directory is reserved for **Kairos protocol bundles**: curated groups of
example or reusable protocol content (distinct from MCP host integrations under
`integrations/` and from installable agent skills under `skills/`).

Generic examples may be added here in the future.

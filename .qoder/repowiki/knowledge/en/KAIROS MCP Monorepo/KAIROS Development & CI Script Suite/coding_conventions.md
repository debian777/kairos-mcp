- Scripts are language-agnostic CLIs invoked directly from Makefile targets or GitHub Actions steps rather than wrapped in a framework.
- Service endpoints are resolved via Docker Compose hostnames (`keycloak`, `qdrant`, `app`) instead of hard-coded ports.
- Secrets and configuration are loaded from a `.env` file generated by `deploy-generate-dev-secrets.py` before any script runs.
Node.js ESM (`import`/`fetch`/`fs/promises`) for journey tooling; Python 3 with `venv` + `pip` for skills validation using the external `skills-ref` package pinned to `git+https://github.com/agentskills/agentskills.git#subdirectory=skills-ref`; TypeScript compiler API (`typescript` package) consumed at runtime by the source-cleanliness scanner; Bash for Qdrant lifecycle and dev shell helpers.
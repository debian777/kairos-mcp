Helm v2 chart targeting Kubernetes with Gateway API as primary ingress mode (auto-detected via `gateway.networking.k8s.io/v1/GatewayClass` lookup when `gateway.mode=auto`); Keycloak managed by the Keycloak operator; Postgres via Percona Distribution PostgreSQL CR; vector store via Qdrant Helm subchart; Redis via Valkey subchart; embedded Python 3.12-alpine script for DCR cleanup using only stdlib (`urllib`, `json`, `datetime`, `os`, `sys`).
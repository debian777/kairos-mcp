Tests skip when not running against a real HTTP backend (`isHttpTransport()` gates `describe`); the CLI/MCP variants additionally require a running Kairos server and a logged-in CLI config (`requireMcpServerAndCliLogin`). The `all_adapters + space_name` test is skipped under `ENV=dev_simple` because it needs the `/shared/ci-test` group space. Before each run the dump root is cleaned via `ensureMimeFixtureExportDumpRootCleanBeforeMimeTests()` so artefacts do not leak between transports.
# Dev environment example. For Keycloak dev profile, use scripts/generate-dev-secrets.sh
# to create .env with random KEYCLOAK_ADMIN_PASSWORD and KEYCLOAK_DB_PASSWORD (never commit .env).
# Copy the rest to .env.dev for the app (see env.example.txt for full options).

# Required for docker compose volume paths (infra + dev profile)
VOLUME_LOCAL_PATH=./data

# Keycloak (profile dev): URL, realm, client — app can use these when auth is wired
KEYCLOAK_DEV_URL=http://localhost:8080
KEYCLOAK_DEV_REALM=kairos-dev
KEYCLOAK_QA_REALM=kairos-qa
KEYCLOAK_PROD_REALM=kairos-prod
KEYCLOAK_DEV_CLIENT_ID=kairos-mcp
# Admin UI (dev only): username admin, password from .env KEYCLOAK_ADMIN_PASSWORD — never in production.

# OIDC Bearer auth (required for MCP OAuth / Bearer token validation)
AUTH_MODE=oidc_bearer
AUTH_TRUSTED_ISSUERS=http://localhost:8080/realms/kairos-dev
AUTH_ALLOWED_AUDIENCES=kairos-mcp
